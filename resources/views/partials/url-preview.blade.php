<small id="url-preview" class="form-hint d-none"
       data-base="{{ url('/') }}"
       data-source="{{ $source }}"
       data-parent="{{ $parent }}"
       data-home-slug="{{ $homeSlug }}">
    <i class="fa-solid fa-link" aria-hidden="true"></i>
    <span></span>
</small>
